
To run the program:

	make 

To recompile:

	make clean && make

To run with arguments:

	./main < infile > outfile
	
		or
	
	./main > outfile < infile

		or

	./main < infile

		or

	./main > outfile

